name: "Check code quality"
description: "Checks code quality: formatters, linters, build, test, etc."
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        bun lint
        bun run build
        bun test
